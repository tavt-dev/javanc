quarkus.log.console.json.enabled=${LOG_JSON_ENABLED:true}
quarkus.micrometer.export.prometheus.enabled=${METRICS_PROMETHEUS_ENABLED:true}

quarkus.http.cors.origins=${GATEWAY_CORS_ORIGINS}
gateway.cors.origins=${GATEWAY_CORS_ORIGINS}

services.user.url=${USER_SERVICE_URL}
services.profile.url=${PROFILE_SERVICE_URL}
services.project.url=${PROJECT_SERVICE_URL}
services.notification.url=${NOTIFICATION_SERVICE_URL}
services.manager.url=${MANAGER_SERVICE_URL}
services.image.url=${IMAGE_SERVICE_URL}
services.email.url=${EMAIL_SERVICE_URL}

quarkus.redis.foundation.hosts=${REDIS_URL}
quarkus.redis.foundation.password=${REDIS_PASSWORD}
quarkus.redis.foundation.tls.enabled=${REDIS_SSL:false}
